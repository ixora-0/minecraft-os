[package]
name = "minecraft-os"
version = "0.1.0"
edition = "2024"
default-run = "qemu-uefi"

[workspace]
# members = ["kernel", "tests-integration"]
members = ["kernel"]

[build-dependencies]
kernel = { path = "kernel", artifact = "bin", target = "x86_64-unknown-none" }
bootloader = "0.11.3"

[dependencies]
ovmf-prebuilt = "0.2.7"
